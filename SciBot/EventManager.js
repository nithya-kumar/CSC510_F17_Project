var request = require('request');
var fs = require("fs");
var Promise = require('bluebird');
var parse = require('parse-link-header');


function createPing(message, user, day, date)
{
	var options = {
		url: https://slack.com/api/reminders.add,
		method: 'POST',
		headers: {
			
		}
	};
}